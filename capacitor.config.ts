import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ludo.game',
  appName: 'multicompanies',
  webDir: 'dist/multicompanies',
  bundledWebRuntime: false
};

export default config;
